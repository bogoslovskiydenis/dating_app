import 'package:flutter/material.dart';

class EmainScreen extends StatelessWidget {
  const EmainScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
